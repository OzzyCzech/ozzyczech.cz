---
interface Props {
	src: ImageMetadata;
	title: string;
	href?: string;
}

const { src, title, href } = Astro.props;
const sourceDomain = href ? new URL(href).hostname.replace('www.', '') : null;
---

<figure class="gallery-item">
	<a class="gallery-image-link" href={src.src} target="_blank">
		<img src={src.src} alt={title} loading="lazy" />
	</a>
	{href && sourceDomain && (
		<a href={href} target="_blank" rel="noopener noreferrer" class="gallery-source">{sourceDomain}</a>
	)}
</figure>

<style>
	.gallery-item {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		margin: 0;
		min-width: 0;
		padding: 1rem;
		border: 1px solid rgba(128, 128, 128, 0.2);
		border-radius: 0.75rem;
		background: rgba(128, 128, 128, 0.03);
	}

	.gallery-image-link {
		display: block;
		line-height: 0;
	}

.gallery-item img {
		display: block;
		width: 100%;
		height: auto;
		border-radius: 0.5rem;
	}

	.gallery-source {
		font-family: system-ui, sans-serif;
		font-size: 0.75rem;
		opacity: 0.5;
		text-decoration: none;
	}

	.gallery-source:hover {
		opacity: 0.8;
		text-decoration: underline;
	}
</style>
